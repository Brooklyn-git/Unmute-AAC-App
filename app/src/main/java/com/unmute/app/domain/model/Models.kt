package com.unmute.app.domain.model

enum class ImageType {
    SYMBOL,
    PHOTO,
    EMOJI,
}

enum class AudioOutput {
    AUTO,
    SPEAKER,
    WIRED,
    BLUETOOTH,
}

enum class AppLanguage {
    SYSTEM,
    EN,
    ES,
}
